<%= form_for(@rider, html: { class: 'form-horizontal' }) do |f| %>
  <% if @rider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rider.errors.count, "error") %> prohibited this rider from being saved:</h2>

      <ul>
      <% @rider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>

  <!--<div class="form-group">
    <%= f.label :fav, 'Favorite', class: 'col-sm-2 control-label' %>
    <div class="col-sm-4  form-control-static">
      <%= f.check_box :fav %>
    </div>
  </div>-->

  <div class="form-group">
    <%= f.label :notes, class: 'col-sm-2 control-label' %>
    <div class="col-sm-4">
      <%= f.text_area :notes, class: 'form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-4">
      <%= f.submit class: 'btn btn-primary' %>
      <%= link_to 'Go Back', riders_path, class: 'btn btn-default pull-right' %>
    </div>
  </div>
<% end %>
