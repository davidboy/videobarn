<h1 class="page-header">
  Editing run
  <small>
      <%= @run.video.original_name %> from <%= @run.video.source %>
      <%= link_to 'This video doesn\'t work', bad_video_path(@run.video), class: 'btn btn-default pull-right' %>
    </small>
</h1>

<%= render 'form' %>

<%= link_to 'Show', @run %> |
<%= link_to 'Back', runs_path %>
